#include <stdio.h>

int main()
{
    int num;
    printf("Enter the number: ");
    scanf("%d",&num);
    char initial = 'k';
    printf("the entered number is %d \n",num);
    printf("the initial of the name is %c \n",initial);
    return 0;
}
