#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

int main()
{
    int r, area;
    printf("Enter the radius of the circle: ");
    scanf("%d",&r);
    area=3.14*r*r;
    -+
    +
    printf("The radius of the circle is: %d",area);
    return 0;
}
