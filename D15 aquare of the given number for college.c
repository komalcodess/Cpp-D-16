#include <stdio.h>

int main()
{
    int num;
    printf("Enter the number you want to calculate square of: ");
    scanf("%d",&num);
    printf("The square of the entered number is %d",num*num);
    return 0;
}
